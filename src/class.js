class Oliveyoung {
  constructor(makeUp, makeTool, eat, hairTool) {
    // this.makeUp = makeUp;
    // this.makeTool = makeTool;
    // this.eat = eat;
    // this.hairTool = hairTool;

    make : {
      this.makeUp = makeUp;
    this.makeTool = makeTool;
    }
    mouse : {
      this.eat = eat;
    }
    hair {
      this.hairTool = hairTool;
    }
    // 지금 문법은 맞지 않지만 이런식으로 구분할 수 있다
    // 자료구조가 달라질 수 있음
  }
}